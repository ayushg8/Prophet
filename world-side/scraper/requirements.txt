# Runtime Python dependencies for isolated scraper collectors/sanitizers.
#
# Keep this public-safe: package names and versions only, no credentialed index
# URLs or private package locations. The current scraper package is stdlib-only,
# so this stays empty on purpose.
