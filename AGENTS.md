# Project Prophet — Agent Instructions

## Overview

Project Prophet is a **documentation/research-only** repository (as of this writing). It contains technical writeups and research for a cybersecurity AI agent concept. There is no source code, no dependency manifests, no tests, and no runnable application.

### Repository contents

- `PROPHET_TECHNICAL_WRITEUP.md` — consolidated technical design
- `research/` — whiteboard sketch, competitive landscape, feasibility, policy risk documents
- `.gitignore` — pre-configured for Python + Node/React (anticipating future code)
- `LICENSE` — MIT

## Cursor Cloud specific instructions

- **No build, lint, test, or run commands exist.** The repository is purely documentation/markdown at this time.
- **No dependencies to install.** There is no `package.json`, `requirements.txt`, `pyproject.toml`, `Makefile`, or any dependency manifest.
- **No services to start.** There are no backend or frontend applications to run.
- **Future code is anticipated** (Python backend with FastAPI, React/TS + Vite frontend) based on the `.gitignore` and technical writeup, but none has been implemented yet.
- If source code is added in the future, update this section with setup, lint, test, and run instructions.
